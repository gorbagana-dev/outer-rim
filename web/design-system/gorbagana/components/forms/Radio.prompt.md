Radio group with hot-pink dot; pink distinguishes single-choice from acid multi-choice checkboxes.

```jsx
<Radio name="speed" value={v} onChange={setV} options={[{value:'fast',label:'Fast'},{value:'faster',label:'Faster',description:'Priority fee in $GOR'}]} />
```
