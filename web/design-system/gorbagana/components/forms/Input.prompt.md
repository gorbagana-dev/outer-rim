Text field on a black "bin" well; cyan glow on focus, pink on error. Use `mono` for addresses/amounts.

```jsx
<Input label="Amount" suffix="$GOR" mono placeholder="0.00" />
<Input iconLeft="search" placeholder="Search tx, block, address…" />
<Input label="Recipient" error="That's not a Gorbagana address" />
```
