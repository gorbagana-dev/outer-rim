Modal over a blurred void scrim; acid-glow marker title, pink eyebrow, actions bottom-right.

```jsx
<Dialog open={open} onClose={close} eyebrow="Confirm" title="Bridge 420 $GOR?" footer={<><Button variant="ghost" onClick={close}>Back</Button><Button>Send it</Button></>}>
  This takes about a block. Trash has finality.
</Dialog>
```

`variant="sticker"` for Trash Council notices / lore interstitials.
