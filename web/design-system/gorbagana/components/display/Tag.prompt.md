Filter chip or paper sticker label — use chips for trait filters, stickers for lore/meme copy on cards.

```jsx
<Tag selected onClick={...}>Teal bin</Tag>
<Tag onRemove={...}>Rainbow eyes</Tag>
<Tag variant="sticker" tilt={-3}>Good trash only</Tag>
```
