Square icon-only button for toolbars, nav bars and card actions; always pass `label`.

```jsx
<IconButton icon="copy" label="Copy address" />
<IconButton icon="x" label="Close" variant="outline" />
<IconButton icon="heart" label="Like" variant="pink" size="sm" />
```
