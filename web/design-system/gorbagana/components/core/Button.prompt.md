Uppercase, tracked, 2px-bordered button; acid green glow is the primary CTA, pink outline is secondary, paper "sticker" is for lore/meme moments.

```jsx
<Button iconRight="arrow-right">Enter the landfill</Button>
<Button variant="secondary">Bridge $GOR</Button>
<Button variant="sticker" size="lg">Good trash only</Button>
<Button variant="ghost" size="sm" iconLeft="copy">Copy CA</Button>
```

One primary per view. Glow intensifies on hover; press scales to .97 (sticker translates into its hard shadow instead). Labels are short imperative verbs — "Build. Dump. Repeat." energy, not "Learn more".
